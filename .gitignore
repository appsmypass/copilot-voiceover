# Local Copilot sign-in cache (per-user, permission-locked) - never commit
account.dat
account.json
*.dat

# Vosk speech models + runtime files (downloaded on first run)
models/
run/

# Python cache
__pycache__/
*.pyc
